# Trivy vulnerability exceptions for xcp_pulse.
#
# One CVE ID per line, with a comment explaining why it's accepted rather
# than fixed. Anything listed here was a deliberate call, not an oversight —
# see SECURITY.md for how to report a vulnerability that shouldn't be here.
